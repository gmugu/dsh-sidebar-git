# Dependencies (build-time only: esbuild, clsx)
node_modules/

# Build churn marker (timestamp written by build/build.js)
lib/.built

# Local npm cache / logs
.npm-cache/
*.log

# Scratch files from a working session (plan documents, test fixtures): never
# published, and a stray `git add -A` must not pick them up.
tmp-*
